import React from "react";

import styles from "./style.module.css";

const Toolbar = () => (
  <header className={styles.Toolbar}>
    <div>...</div>
    <div className={styles.Content}>Logo</div>
    <nav>Menu</nav>
  </header>
);

export default Toolbar;
